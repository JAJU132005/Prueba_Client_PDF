export function Footer(): JSX.Element {
  return (
    <footer className="border-t border-border bg-surface">
      <div className="mx-auto max-w-6xl px-4 py-6 text-sm text-text-muted md:px-6">
        Tus archivos nunca salen de tu navegador · sin subida · sin registro
      </div>
    </footer>
  );
}
